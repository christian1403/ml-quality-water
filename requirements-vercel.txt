# Vercel-optimized requirements for TensorFlow Lite deployment
# Lightweight dependencies for serverless deployment

# FastAPI and server dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Core data processing (lightweight versions)
numpy>=1.21.0,<1.26.0  # Pin to avoid compatibility issues
pandas>=1.5.0,<2.0.0   # Pin to avoid size issues
scikit-learn>=1.3.0,<1.4.0  # Pin for stability

# TensorFlow Lite runtime (much smaller than full TensorFlow)
tflite-runtime>=2.13.0  # Lightweight TFLite interpreter

# Alternative: Use ai-edge-litert if available
# ai-edge-litert>=0.1.0

# Other core dependencies
joblib>=1.1.0
python-dotenv>=0.19.0
google-generativeai>=0.3.0

# Data science utilities (lightweight)
imbalanced-learn>=0.10.0

# Optional: Remove heavy dependencies for Vercel
# matplotlib  # Remove for smaller deployment
# seaborn     # Remove for smaller deployment
