# Requirements for TensorFlow Lite deployment (Vercel compatible)
# Lightweight dependencies for serverless deployment

# Core API dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Data processing (lightweight versions)
numpy>=1.21.0,<1.25.0
pandas>=1.5.0,<2.1.0
scikit-learn>=1.3.0,<1.4.0

# TensorFlow Lite runtime (much smaller than full TensorFlow)
tflite-runtime>=2.13.0

# Other lightweight dependencies
joblib>=1.1.0
python-dotenv>=0.19.0

# AI integration
google-generativeai>=0.3.0

# Optional: for development and model conversion only
# tensorflow>=2.15.0  # Uncomment only for development/training
